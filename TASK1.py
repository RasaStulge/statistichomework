# TASK 1
# Create a numpy array with values from 1 to 9. Display and then invert this array(first element becomes last, etc.)
# Reverse

import numpy as np

my_list = np.arange(1, 10)
print(my_list)
print(my_list[::-1])