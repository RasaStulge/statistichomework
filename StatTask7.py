# Create any array of values and then calculate the mean and the median.

import numpy as np

arr = np.array([23, 45, 112, 150, 43, 254, 95, 8])
print(arr)

mean = np.mean(arr)
print(mean)

median = np.median(arr)
print(median)